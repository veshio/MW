# Spotify API Credentials
# Get these from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# OAuth Redirect URI
# MUST match exactly what's registered in Spotify Dashboard
# Development: http://127.0.0.1:3001/api/auth/callback
# Production: https://your-backend-domain.com/api/auth/callback
SPOTIFY_REDIRECT_URI=http://127.0.0.1:3001/api/auth/callback

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS and OAuth redirect after login)
# Development: http://127.0.0.1:5173
# Production: https://your-frontend-domain.com
FRONTEND_URL=http://127.0.0.1:5173

# IMPORTANT NOTES:
# 1. Use 127.0.0.1 (not localhost) for local development - Spotify OAuth requirement
# 2. Add all redirect URIs to Spotify Dashboard: https://developer.spotify.com/dashboard
# 3. For production, update SPOTIFY_REDIRECT_URI and FRONTEND_URL to deployed URLs
# 4. Backend and frontend can be on different domains (they communicate via CORS)
